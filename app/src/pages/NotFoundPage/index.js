import React from 'react';
import Section from '../../components/Section';

export default () => (
  <Section style={ { minHeight: '400px' } }>
    <h5>Page Not Found</h5>
    <p>The page you navigated to does not exists.</p>
  </Section>
);
